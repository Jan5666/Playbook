# Secrets & local-only files — NEVER commit.
# The GitHub Pages deploy uploads the whole repo, so anything tracked is PUBLIC.
# (.gitignore does NOT support inline comments — keep patterns on their own line.)
stooq_cookies.txt
settings.local.json
.env
.env.*
*.local
*.pem
*.key

# Build output & tooling
dist/
node_modules/

# Subagent-driven-development scratch (progress ledger, briefs, review packages)
.superpowers/

# Throwaway per-increment slice/verify scripts (never meant to be committed)
scratchpad/

# OS / editor cruft
.DS_Store
Thumbs.db

# Logo pack contact sheet (human-review artifact, not shipped)
logos/contact-sheet.html
